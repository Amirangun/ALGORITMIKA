using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Cybermumutimumutimur : MonoBehaviour
{
    public rigiddody rb;
    public float right;
    public float left;
    public float speed = 5;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        if Input(GetKey.(KeyCode.A));
            transform.translate speed * left * Time.deltaTime
    }
}
